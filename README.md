# ENV Var Confessional

A simple tool that checks your environment variables before runtime. Catches missing, misnamed, or incorrectly typed variables so they don't surprise you later. Run: `node env-confessional.js` or `node env-confessional.js .env.local`